/* Input string :  "SaLeS" => "sAlEs"
 

 */

public static class ToggleCharacters
{
    public static string solve(string A)
    {
        string res = string.Empty;
        char[] chars = A.ToCharArray();

        for (int i = 0; i < A.Length; i++) {
            //chars[i] ^= 32;
        }

        return res;
    }
}